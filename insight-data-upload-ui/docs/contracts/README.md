# Backend Contract Directory

`openapi.yaml` is the machine-readable backend contract used by frontend AI agents and developers.

Before adding/changing an HTTP API:

1. Search this OpenAPI document.
2. Verify the method/path/schema.
3. Update the typed Angular API boundary.
4. Add/update focused tests.

Do not invent endpoints that are not represented by the contract.
